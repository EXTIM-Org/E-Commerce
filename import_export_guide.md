# راهنمای آپلود محصولات با اکسل (Import Products)

این پروژه از سیستم یکپارچه برای آپلود همزمان **محصولات اصلی** و **متغیرهای آن‌ها (Variants)** پشتیبانی می‌کند.
با استفاده از این قابلیت می‌توانید صدها محصول و متغیر را تنها با آپلود یک فایل اکسل (یا CSV) به سیستم اضافه کنید.

## قوانین کلی
- فایل شما باید دقیقاً شامل ستون‌های (Header) مشخص شده در جدول زیر باشد. نام ستون‌ها باید **دقیقاً** مطابق متن‌های نوشته شده باشند (حتی فاصله‌ها و پرانتزها).
- سیستم بر اساس **«کد متغیر (SKU)»** کار می‌کند. این کد باید برای هر خط کاملاً یکتا و اختصاصی باشد (نمی‌تواند تکراری باشد).
- اگر **«شناسه محصول (Slug)»** در دیتابیس وجود نداشته باشد، سیستم به صورت خودکار یک محصول جدید با همان شناسه می‌سازد و سپس متغیر (خط فعلی) را به آن متصل می‌کند.
- اگر **«شناسه محصول (Slug)»** در دیتابیس وجود داشته باشد، سیستم فرض می‌کند شما می‌خواهید یک متغیر جدید (رنگ یا سایز جدید) به محصول قبلی اضافه کنید. در این حالت می‌توانید ستون‌های نام کالا، برند و دسته‌بندی را خالی بگذارید.

## ساختار فایل اکسل

| شناسه محصول (Slug) | نام کالا | دسته‌بندی | برند | کد متغیر (SKU) | ویژگی (مثل رنگ/سایز) | قیمت متغیر | موجودی |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `iphone-15-pro` | آیفون 15 پرو | موبایل | اپل | `IPH-15P-W-256` | سفید - 256GB | 65000000 | 12 |
| `iphone-15-pro` | | | | `IPH-15P-B-512` | مشکی - 512GB | 72000000 | 4 |
| `airpods-pro-2` | ایرپاد پرو ۲ | لوازم جانبی | اپل | `AIR-PRO2-W` | سفید | 12000000 | 30 |

### توضیحات ستون‌ها:
1. **شناسه محصول (Slug) (الزامی):** نامک یا شناسه یکتای محصول اصلی که در آدرس URL قرار می‌گیرد (فقط حروف انگلیسی، خط تیره و اعداد مجاز است).
2. **نام کالا:** نام اصلی محصول که به مشتری نمایش داده می‌شود (فقط برای اولین متغیرِ هر محصول الزامی است).
3. **دسته‌بندی:** نام دسته‌بندی (مثل "لپ‌تاپ"). اگر در سایت وجود نداشته باشد به صورت خودکار ساخته می‌شود!
4. **برند:** نام برند محصول (مثل "سامسونگ"). در صورت عدم وجود به صورت خودکار ساخته می‌شود.
5. **کد متغیر (SKU) (الزامی):** یک کد کاملاً منحصربه‌فرد برای این کانفیگ خاص (مثلاً برای آیفون مشکی ۱۲۸ گیگ). این شناسه نباید در هیچ کجای سایت تکرار شود.
6. **ویژگی (مثل رنگ/سایز) (الزامی):** نامی که این متغیر را از سایر متغیرهای محصول متمایز می‌کند (مثل "مشکی" یا "سایز XL").
7. **قیمت متغیر (الزامی):** قیمت نهایی فروش این متغیر (فقط عدد، بدون کاما یا تومان).
8. **موجودی (اختیاری):** تعداد موجودی این کالا در انبار. اگر خالی بگذارید، 0 در نظر گرفته می‌شود.

## روش انجام کار در سایت
۱. فایل اکسل خود را آماده کنید (یا ترجیحاً با پسوند `.csv` با انکودینگ `UTF-8` ذخیره کنید).
۲. وارد پنل ادمین شده و به بخش **محصولات** بروید.
۳. روی دکمه‌ی **Import** (در بالای صفحه سمت راست) کلیک کنید.
۴. فایل خود را انتخاب کرده و دکمه تایید را بزنید.
۵. سیستم یک پیش‌نمایش (Preview) از کارهایی که قرار است انجام دهد به شما نشان می‌دهد. اگر خطایی (Error) با رنگ قرمز نبود، دکمه‌ی نهایی **Confirm Import** را بزنید تا اطلاعات وارد دیتابیس شوند.
