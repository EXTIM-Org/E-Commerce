در حالت فعلی، سیستم واردات (Import) کاملاً هوشمند عمل می‌کند و اگر اطلاعاتی را آپلود کنید که قبلاً وجود داشته باشند، تداخلی ایجاد نمی‌شود؛ بلکه بروزرسانی (Update) انجام می‌شود.

اتفاقی که می‌افتد بستگی به این دارد که کدام فایل را آپلود می‌کنید:

۱. فایل محصولات (product_sample.csv یا همان متغیرها)
معیار شناسایی سیستم در این فایل، «کد کالا (SKU)» و «شناسه محصول (Slug)» است:

متغیرها (مثل قیمت و موجودی): اگر SKU ای که در فایل نوشتید در دیتابیس وجود داشته باشد، سیستم متغیر جدید نمی‌سازد؛ بلکه اطلاعات همان متغیرِ قبلی (مثل تغییر قیمت یا تعداد موجودی انبار) را با مقادیر جدیدِ فایل بروزرسانی می‌کند.
خود محصول (مانند نام، دسته‌بندی و برند): سیستم محصول را از روی شناسه (Slug) پیدا می‌کند. اگر محصول وجود داشته باشد، نام یا دسته‌بندیِ فعلی آن را بازنویسی نمی‌کند (مگر اینکه فیلدهای آن در دیتابیس خالی بوده باشند که در این صورت با مقادیر فایل پر می‌شوند).
۲. بقیه فایل‌ها (مثل دسته‌بندی‌ها، کاربران، برندها و...)
در این فایل‌ها معیار شناسایی، ستون ID است:

اگر ستون ID را خالی بگذارید: سیستم فرض می‌کند این یک مورد کاملاً جدید است و آن را در دیتابیس ایجاد (Create) می‌کند.
اگر ستون ID را با شناسه عددی یک رکورد که الان وجود دارد پر کنید: سیستم آن رکورد را پیدا کرده و دقیقاً اطلاعاتش را با مقادیر فایل شما ویرایش (Update) می‌کند.
نتیجه‌گیری: شما با خیال راحت می‌توانید از این قابلیت هم برای وارد کردن اطلاعات جدید به صورت گروهی و هم برای ویرایش گروهی اطلاعات فعلی (مثلاً تغییر قیمت ۱۰۰ محصول به صورت همزمان یا بروزرسانی موجودی انبار) استفاده کنید.